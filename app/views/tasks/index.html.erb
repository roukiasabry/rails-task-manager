<h1>MY TASKS:</h1>

<% @tasks.each do |task|%>
  <p><input type="checkbox" <%= "checked" if task.completed %>><%= link_to task.title, task_path(task) %>  <%= link_to "🗑️",
  task,
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
  %></p>
<% end %>

<p><%= link_to "+ Add a new task", new_path %></p>
